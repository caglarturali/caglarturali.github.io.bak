/**
 * Contact component styles.
 */
import { Styles } from 'react-jss';
import { mSize } from '../../theme';

export default {
  contactItem: {
    fontSize: mSize,
    marginBottom: '10px',
  },
} as Styles;
